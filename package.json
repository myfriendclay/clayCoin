{
  "scripts": {
    "heroku-prebuild": "cd backend && npm install",
    "start": "cd backend && npm run start"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.1",
    "@types/node": "^20.14.10"
  }
}
